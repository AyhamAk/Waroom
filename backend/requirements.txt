fastapi==0.115.4
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
langgraph==0.2.53
langchain-core==0.3.20
anthropic==0.40.0
pydantic==2.10.3
sse-starlette==2.1.3
aiofiles==24.1.0
duckduckgo-search==6.3.7
google-generativeai==0.8.3
httpx==0.27.2
# Blender Studio pipeline optional dependencies
# Install with: pip install mss Pillow pywin32
mss>=9.0.1
Pillow>=10.0.0
pywin32>=306; sys_platform == "win32"

# Game Studio pipeline — Vision Playtester drives the build via a real
# browser. Install browsers after pip with:  playwright install chromium
playwright>=1.48.0

# Pause / resume across server restarts — LangGraph sqlite checkpointer.
# Pin to 2.x line: langgraph 0.2.53 requires langgraph-checkpoint <3.0.
# aiosqlite must be <0.20: newer aiosqlite removed an internal Connection
# attribute that langgraph-checkpoint-sqlite 2.0.11 still calls
# (`'Connection' object has no attribute 'is_alive'`). 0.19 works.
langgraph-checkpoint-sqlite>=2.0.0,<3.0.0
langgraph-checkpoint>=2.0.0,<3.0.0
aiosqlite>=0.19,<0.20
